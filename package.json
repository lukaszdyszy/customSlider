{
  "name": "customslider",
  "version": "1.0.0",
  "description": "Simple customizable universal javascript slider",
  "main": "dist/customSlider.min.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src/index.js -o dist/customSlider.js && babel src/index.js --presets minify -o dist/customSlider.min.js"
  },
  "keywords": [
    "slider",
    "javascript"
  ],
  "author": "lukaszdyszy",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.5.5",
    "@babel/core": "^7.5.5",
    "@babel/preset-env": "^7.5.5",
    "babel-preset-minify": "^0.5.0"
  }
}
